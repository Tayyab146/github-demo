apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: app-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: SecretStore
    name: aws-secretsmanager
  target:
    name: app-secret
    creationPolicy: Owner
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: /app/staging/database
        property: url
    - secretKey: REDIS_PASSWORD
      remoteRef:
        key: /app/staging/redis
        property: password
    - secretKey: JWT_SECRET
      remoteRef:
        key: /app/staging/auth
        property: jwtSecret
